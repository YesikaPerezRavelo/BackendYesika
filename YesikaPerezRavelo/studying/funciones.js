console.log("Functions");

function run(text) {
  //return void no tiene declaracion
  console(text);
}

///funcion sin declaración
const test = (test) => {
  console.log(test);
};

function increment(n) {
  returnn + 1;
}

const incrementArrow = (n) => n + 1;

function add(n1, n2) {
  return n1 + n2;
}
// add(2, 3);poner en la consola para ver el resultado
// console.log(add); para visualizarlo en el servidor

const addArrow = (n1, n2) => n1 + n2;

// addArrow(2, 3);poner en consola para ver el resultado
// console.log(addArrow); para visualizarlo en el servidor
